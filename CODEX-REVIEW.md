# Codex 독립 검증·개선 의뢰서

아래 전문을 그대로 Codex에 붙여넣으면 됩니다.

---

## 역할

너는 이 저장소를 처음 보는 **독립 검토자**다. 기존 구현자의 판단을 신뢰하지 말고
직접 코드를 읽고, 직접 실행해서 확인해라. "아마 될 것이다"는 결론으로 인정하지 않는다.

목표는 두 가지다.
1. **버그·회귀 찾기** — 특히 최근 6개 커밋에서 들어간 변경 주변
2. **실제로 고치기** — 찾은 것 중 확실한 것은 수정하고, 애매한 것은 보고만

## 대상

```
https://github.com/formars0309-cloud/isekai-alpha
```

게임 본체는 `alphatalk/` 폴더다. 저장소 루트의 `index.html`, `js/`, `css/`와
`alphatalk-solo-backup/`은 **보존용 구버전이므로 건드리지 마라.**

빌드 도구가 없다. 정적 파일이므로 아래로 바로 실행된다.

```bash
cd alphatalk && python -m http.server 8000
# http://localhost:8000/?debug=1  ← debug=1 이면 좌상단에 개발자 패널이 뜬다
```

배포본: https://formars0309-cloud.github.io/isekai-alpha/alphatalk/

## 이 게임이 무엇인가 (톤을 망치지 않으려면 반드시 읽어라)

한국 남성향 연애 시뮬레이션이다. 이세계로 전이된 주인공에게 **왕국 기사단장 엘리시아가
먼저 메시지를 보내온다.** 플레이어는 답장을 고르기만 한다. 건국제 무도회까지 7일.

- **메신저(카카오톡) UI를 흉내낸 포맷**이다. 타이핑 인디케이터, 읽음 표시, 사진 전송이 핵심 연출이다.
- 엘리시아는 **츤데레 말투**다. 말줄임표가 잦고, "…그뿐이다", "…기록하지 마라" 같은
  퉁명스러운 부정 뒤에 진심을 숨긴다. **이 말투를 절대 평범하게 다듬지 마라.**
- **15세 이용가**를 유지한다. 노골적 성적 묘사 금지, 은유까지만.
- 세로 모바일 전용이다. 데스크톱 레이아웃은 고려 대상이 아니다.

## 아키텍처

```
alphatalk/js/
  heroines.js   히로인 레지스트리. 엔진은 이 파일만 보고 돈다 (특정 히로인을 모른다)
  data.js       엘리시아 7일치 본 대본(A안) · 낮 일과 카드 · 무드/스타일 정의
  variants.js   대사 변주 B·C안
  variants2.js  대사 변주 D·E안 (VARIANTS 에 병합)
  endings.js    엔딩 구간 7종 · 후일담(스타일축·스탯축)
  engine.js     메신저 엔진 · 상태 · 저장 · 렌더
  ai.js         Claude API 로 대사를 생성하는 선택 기능 (기본 OFF)
  portraits.js  SVG 초상화 (아바타 이미지가 없을 때의 폴백)
  audio.js      Web Audio 합성 효과음
```

**대본 노드 타입**: `{e}` 그녀 대사 · `{me}` 내 강제발화 · `{c}` 중앙지문 · `{bot}` 봇방 중계
· `{photo, cap?}` 사진 전송 · `{emo}` 표정 변경 · `{status}` 상태메시지 · `{branch}` 조건분기
· `{choices:[{t, style, req?, flag?, ignore?, then:[]}]}` 선택지

## 절대 깨면 안 되는 계약

여기를 어기면 게임이 조용히 망가진다. **수정 후 반드시 재확인해라.**

1. **선택지 스타일**: 모든 선택지 묶음은 `bold`(직진) / `wit`(재치) / `care`(배려) 각 1개씩,
   정확히 3개다. (예외: 읽씹용 `ignore` 1개 추가 가능, D7 밤 고백은 `final`/`end` 노드)
2. **무드 매칭**: `proud→wit`, `lonely→bold`, `tired→care`. 엘리시아 무드는
   `1 proud · 2 lonely · 3 tired · 4 lonely · 5 tired · 6 proud · 7 lonely`.
   각 묶음에는 **그날 적중 스타일이 반드시 하나 있어야 한다.**
3. **구조 플래그**: D5밤 bold=`req str 12` + 전 선택지 `flag:"d5good"` / ignore=`d5bad`,
   D6밤 bold=`req gold 30` + `suit:true`, D1·D3밤 ignore=`flag:"hurt"`.
4. **밸런스 수치**: 성사선 **80**. 밤 적중 D1~D6 = 7/8/10/12/14/16, 아침 적중 = 2/3/3/4/5/5/6,
   빗나감은 밤 2·아침 1. **이 숫자를 임의로 바꾸지 마라.**
5. **변주는 묶음 단위**다. A~E안의 선택지를 서로 교차 조합하면 문맥이 붕괴한다.
6. **엔딩 구간**: 90+ `truelove` / 80~89 `ok` / 65~79 `near` / 45~64 `fail` / ~44 `stranger`
   / 신청안함·65+ `night` / 그외 `chicken`.

계약 검사는 브라우저 콘솔에서 전 변주를 순회하며 자동으로 돌릴 수 있다.
`DAYS`, `VARIANTS`, `ENDINGS`, `HEROINES` 가 전역에 있으니 직접 스크립트를 짜서 검증해라.

## 검증 방법

- `?debug=1` 개발자 패널: 스탯/호감 조작, 슬롯 리필, 하루 건너뛰기, D7 점프,
  엔딩 7종 즉시 재생, 🔎후일담(현재 판정 표시)
- 세이브는 `localStorage["alphatalk-save-v2"]`. 직접 수정하고 새로고침하면 원하는 상태로 갈 수 있다.
  특정 변주를 강제하려면 `S.h.ely.pending = VARIANTS[5].night[3]` 처럼 주입하고
  `pendingChoices=null`, `used={morning:[],night:[]}` 로 맞춘 뒤 reload → 이어하기.
- 변주 뽑기 이력은 `localStorage["alphatalk-seen-variants"]` 에 남는다(직전 판 회피용).

## 중점 검사 영역

아래는 **구현자가 검증하지 못했거나 의심하는 지점**이다. 여기부터 파고들어라.

1. **세이브 호환성**. 최근에 `S.endedSuccess` 필드가 추가됐다. 배포본에서 이미 플레이 중인
   구버전 세이브에는 이 필드가 없다. 엔딩 화면 복원(`S.ended`로 바로 진입하는 경로)과
   `shareText()`의 `outcomeMark()`가 `undefined`를 만났을 때 어떻게 되는지 확인해라.
2. **AI 모드(`ai.js`)와 신규 노드의 상호작용**. AI가 생성한 대사에는 `photo` 노드가 없다.
   AI 모드가 켜진 상태에서 사진이 통째로 사라지는지, 폴백 경로는 정상인지.
   또 AI 응답 스키마가 최근 변경(엔딩 7종 등)을 반영하는지.
3. **읽씹(ignore) 경로 × 사진**. 답장을 안 하면 다음날 `IGNORE_MORNING` 회복 대화가
   앞에 붙는다. 사진이 들어간 날에 이게 겹치면 슬롯/스크롤/표정이 어떻게 되는지.
4. **ResizeObserver 스크롤 유지**(`initScrollKeeper`). 답장 바가 열릴 때 대화 영역이
   줄어드는 것을 감지해 하단에 다시 붙이는 장치다. 사용자가 위로 스크롤해 옛 대화를
   읽는 중에 새 메시지가 오면 방해하지 않아야 한다. 폴딩 버튼(`TAIL=14`)과의 상호작용도 봐라.
5. **이미지 총량**. `alphatalk/img/`에 20장 가까이 있다. 모바일 3G에서 첫 로딩과
   대화 중 사진 도착이 체감상 견딜 만한지. 필요하면 지연 로딩 전략을 제안해라
   (단, `loading="lazy"`는 이 스크롤 컨테이너에서 로드가 안 돼 이미 제거한 이력이 있다).
6. **엔딩 화면 외 다른 화면의 오버플로**. `#scr-ending`은 `flex-direction:column`인데
   `justify-content:center` 때문에 내용이 길면 위쪽이 잘리고 스크롤로도 접근이 불가능했다.
   **같은 패턴이 다른 화면(`.screen`)에도 있는지 전수 확인해라.**
7. **접근성·견고성**. 키보드 조작, `aria` 속성, 이미지 `alt`, 색 대비.
   그리고 저장 실패(사파리 프라이빗 모드 등)나 `localStorage` 용량 초과 시의 동작.

## 하지 말 것

- **대사 톤을 다듬지 마라.** 말줄임표, 퉁명스러운 어미, 문장 부호는 의도된 것이다.
- **밸런스 수치와 구조 계약을 임의로 바꾸지 마라.** 문제를 발견하면 고치지 말고 **보고**해라.
- **대규모 리팩터링 금지.** 빌드 도구 도입, 프레임워크 도입, 파일 구조 개편 모두 하지 마라.
  바닐라 JS + 빌드 없음은 이 프로젝트의 의도적 선택이다.
- 루트의 구버전(`index.html`, `js/`, `css/`)과 `alphatalk-solo-backup/`은 건드리지 마라.
- 한국어 콘텐츠를 영어로 바꾸지 마라.

## 산출물

1. **수정한 것**: 브랜치를 하나 파서 커밋해라. 커밋 메시지는 한국어로,
   무엇이 왜 문제였는지 한 문단 이상 설명해라.
2. **보고서**: 아래 형식으로.
   - 확인된 버그: 재현 절차 + 원인 + 수정 여부
   - 의심되지만 확정 못 한 것: 왜 확정 못 했는지
   - 계약 위반 가능성: 발견 시 고치지 말고 여기에
   - 개선 제안: 우선순위와 예상 영향
3. **검증 근거**: "코드를 읽어보니 괜찮아 보인다"는 근거로 인정하지 않는다.
   실제로 실행하고 확인한 결과를 적어라.
